import React from 'react';

let Homepage = React.createClass({

    render() {
        return(<div>This is the homepage. There will be a Map on the left and a list on the right maybe</div>);
    }
});

export default Homepage;